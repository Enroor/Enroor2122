let alt_cm = 173;
let alt_m = 1.73;
let peso = 64.1;

let alt_round = Math.ceil(alt_m);
let peso_round = Math.floor(peso);

let iguales = Number.MAX_VALUE + 1 === Number.MAX_VALUE;

console.log(iguales);